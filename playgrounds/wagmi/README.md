# Wagmi Example

The Wagmi nuxt module doesn't install the wagmi plugin. 
In order to install it and properly integrate, you will need to use `vue-query.config.ts`, which will be run in the nuxt plugin that install vue query.
